<div class="admin-inner-content">
  <div class="admin-inner-content-title">News</div>
  <div class="admin-inner-content-info">
    <%= raw msg_flash(flash) %>
    <%= form_for([:admin, @news], :url => admin_news_index_path,
                 :html => { :multipart => true}) do |f| %>
      <%= f.error_messages %>
      <%= render :partial => 'form', :locals => { :f => f} %>
      <div class="field">
        <%= f.submit 'Create News' %>&nbsp;&nbsp;
        <%= link_to 'Back', admin_news_index_path %>
      </div>
    <% end %>
    <div class="clear"></div>
  </div>
</div>
